# 开发环境配置
# API base：留空 → 用 vite proxy `/api → localhost:3000`
# 若需绕过 proxy 直连远端，填 http://192.168.1.10:3000 之类
VITE_API_BASE_URL=

# Mock 开关：1 = 所有 api/*.ts 走内置 mock，0 = 走真后端
# 后端 endpoint 还没全部就位时保持 1
VITE_API_MOCK=1
